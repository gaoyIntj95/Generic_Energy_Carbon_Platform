# 通用能源与碳管理平台 — ChatGPT 交接文档

> 生成日期：2026-09-01
> 项目路径：`D:\Project\Generic_Energy_Carbon_Platform`

## 0. 当前任务状态（2026-09-09）

- 当前分支：`codex/energy-flow-simplification`
- 最新提交：`f7651fe feat: simplify energy conversion and flow maintenance`
- 备份分支：`codex/backup-energy-flow-20260909`，基线提交 `e6ed1b3`
- 已完成“能源转换与流向”一期原型改造：旧 recovery/conversion/external 入口统一进入一张维护表；无二级利用录入；一期暂不提供 Excel 导入。
- 设备产出作为能源类设备的权威产出来源，同时供设备指标和能流分析引用；已有来源缺失时从转换行跳转设备产出页并带回返回地址。
- 核心测试：`tests/energyFlowMaintenance.test.ts` 与 `tests/energyFlowDataContract.test.ts` 共 25 项通过；`npm run typecheck` 和 `npm run build` 通过。
- 浏览器已验证：锅炉行内展开、来源查看、设备产出跳转、新增外供及外供后厂内可供量扣减。
- 详细业务口径见 `docs/12-energy-flow-maintenance.md`。

新窗口继续时，先切换到上述改造分支，打开 `/data-management/energy-data?tab=conversion` 查看原型；重点确认页面字段、外供与回收补录的交互是否符合业务习惯，再决定是否继续调整。

## 1. 交接目的

本文档用于把本地项目的背景、协作规则和接手流程交给无法直接访问本地工作区的网页版 ChatGPT。

网页版 ChatGPT 看不到本机项目目录，因此仅上传本文档时，它只能理解项目背景，不能准确分析当前代码实现。若需要代码级分析，应同时上传相关源码、业务文档、截图或项目压缩包。

## 2. 项目概况

- 项目名称：Generic Energy Carbon Platform
- 中文名称：通用能源与碳管理平台
- 项目目标：建设能源管理与碳管理相关的平台，支持能源数据、碳排放数据、指标展示及相关管理流程。
- 当前工作区：`D:\Project\Generic_Energy_Carbon_Platform`

## 3. 项目目录约定

- `docs/`：业务规则、产品需求和页面约束。业务判断应优先以此处文档为准。
- `reference/`：参考页面、视觉参考和截图。
- `src/`：前端源代码。
- `src/modules/`：业务模块目录。新增模块时应遵循现有分组和命名方式。
- `tests/`：测试代码。

接手时还应检查根目录中的 `README`、`package.json`、构建配置、路由入口和环境配置文件。

## 4. 必须遵守的协作规则

1. 修改前先阅读与目标页面相关的 `docs/` 文档。
2. 使用 `reference/` 中的截图或页面作为视觉和交互对照。
3. 新增模块时保持 `src/modules/` 的既有目录结构和命名习惯。
4. 不要擅自进行大范围重构。
5. 不要修改与当前目标无关的文件。
6. 保留已有用户改动，不执行 `git reset --hard`、`git checkout --` 等破坏性命令。
7. 修改后运行适用的 lint、test、build 或其他项目验证命令。
8. 如果业务规则、截图和现有代码不一致，应先指出冲突并说明采用的依据。

## 5. 推荐的接手流程

### 第一步：确认项目状态

检查：

- 当前 git 分支和未提交修改
- 项目使用的框架、语言和构建工具
- 启动、测试、构建命令
- 当前页面入口、路由和模块结构

### 第二步：理解业务和页面

重点阅读：

- `docs/` 中与目标功能相关的需求和业务规则
- `reference/` 中的参考截图或页面
- 页面入口组件及其依赖组件
- mock 数据、API 层、状态管理和类型定义
- 现有测试

### 第三步：形成分析结论

请先输出以下内容，再开始修改：

- 当前技术栈
- 当前页面和模块结构
- 已完成的功能
- 尚未完成的功能
- 发现的问题、风险和假设
- 推荐实施顺序
- 需要用户确认的关键决策

### 第四步：实施和验证

- 仅修改目标范围内的文件
- 优先复用现有组件、样式和数据结构
- 同步补充必要测试
- 运行测试和构建
- 汇报修改文件、验证结果和剩余风险

## 6. 上传给网页版 ChatGPT 的推荐材料

### 最佳方式：上传项目压缩包

将项目打包为 zip 后上传，但建议排除：

- `node_modules/`
- `dist/`、`build/`
- `.git/`
- 本地密钥、token、`.env` 中的敏感信息
- 大型缓存和临时文件

同时上传本文档，告诉 ChatGPT：

> 请先阅读 HANDOFF_FOR_CHATGPT.md，再分析压缩包中的 docs、reference、src 和 tests。不要直接修改代码，先输出当前状态和实施计划。

### 代码量较大时：分批上传

建议按以下顺序上传：

1. `HANDOFF_FOR_CHATGPT.md`
2. `docs/` 相关文档
3. `reference/` 相关截图
4. 根目录配置文件和入口文件
5. 目标页面及其依赖组件
6. `tests/` 中相关测试

不要一开始上传整个 `src/`，否则上下文容易被无关代码占满。

### 只想让 ChatGPT 做方案分析

上传：

- 本文档
- 相关业务文档
- 页面截图
- 目标页面源码
- 相关类型/API/mock 文件

### 想让 ChatGPT 修改代码

网页版 ChatGPT 通常无法直接改动本地工作区。更实际的流程是：

1. 上传相关文件。
2. 让 ChatGPT 输出完整的修改方案或 unified diff/patch。
3. 在本地应用 patch。
4. 运行测试和构建。
5. 将报错日志或修改后的相关文件重新上传，让 ChatGPT 继续复核。

## 7. 推荐的首条提示词

```text
请先完整阅读 HANDOFF_FOR_CHATGPT.md 以及我上传的项目文件。

你无法访问我的本地目录，所以只能基于上传内容分析，不要假设未上传的文件内容。
请先不要修改代码，先输出：
1. 项目技术栈和目录结构；
2. 当前页面的功能和组件关系；
3. 业务规则与页面实现之间的对应关系；
4. 已完成、未完成和疑似问题；
5. 风险、假设和缺失文件；
6. 推荐实施计划。

如果需要更多文件，请明确列出文件路径和原因。除非我确认，否则不要大范围重构。
```

## 8. 当前交接限制

本交接文档记录了项目背景和协作约束，但不等同于完整代码快照。接手模型必须以实际上传的文件为准；没有上传的页面、接口、配置和测试内容都应视为未知，不得自行臆测。
