import { FileBlob, SpreadsheetFile } from "@oai/artifact-tool";
const p = "D:/Project/Generic_Energy_Carbon_Platform/outputs/20260914-data-management/用能单元与能源管理配置表-数据管理功能清单-已调整.xlsx";
const wb = await SpreadsheetFile.importXlsx(await FileBlob.load(p));
const check = await wb.inspect({ kind: "table", range: "数据管理!D12:E27", include: "values,formulas", tableMaxRows: 16, tableMaxCols: 2, maxChars: 8000 });
console.log(check.ndjson);
const errors = await wb.inspect({ kind: "match", searchTerm: "#REF!|#DIV/0!|#VALUE!|#NAME\\?|#N/A|#NUM!|#NULL!|#SPILL!|#CALC!", options: { useRegex: true, maxResults: 100 }, summary: "saved workbook formula error scan" });
console.log(errors.ndjson);
