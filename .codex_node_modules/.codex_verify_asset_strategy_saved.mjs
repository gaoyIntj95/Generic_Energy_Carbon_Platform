import { FileBlob, SpreadsheetFile } from "@oai/artifact-tool";
const p = "D:/Project/Generic_Energy_Carbon_Platform/outputs/20260914-asset-strategy/能碳资产运营与策略分析-功能清单-已调整.xlsx";
const wb = await SpreadsheetFile.importXlsx(await FileBlob.load(p));
console.log((await wb.inspect({kind:"table", range:"能碳资产运营!D15:E34", include:"values,formulas", tableMaxRows:20, tableMaxCols:2, maxChars:10000})).ndjson);
console.log((await wb.inspect({kind:"match", searchTerm:"#REF!|#DIV/0!|#VALUE!|#NAME\\?|#N/A|#NUM!|#NULL!|#SPILL!|#CALC!", options:{useRegex:true,maxResults:100}, summary:"saved workbook formula error scan"})).ndjson);
